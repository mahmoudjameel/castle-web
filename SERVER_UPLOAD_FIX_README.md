# إصلاح مشكلة رفع الفيديوهات على السيرفر المنشور

## المشكلة
عند رفع الفيديوهات من السيرفر المنشور (toqtalent.com)، يظهر خطأ:
```
POST https://toqtalent.com/api/portfolio 413 (Request Entity Too Large)
SyntaxError: Unexpected token '<', "<html>..." is not valid JSON
```

## السبب
الخادم المنشور كان له حدود أصغر لحجم الطلبات مقارنة بـ localhost.

## الحل المطبق (✅ بدون قيود على الحجم)

### 1. إعدادات Next.js (✅ تم تطبيقها)
- `next.config.ts`: إزالة جميع قيود حجم الطلبات (`sizeLimit: '0'`)
- `middleware.ts`: إزالة التحقق من حجم الملفات
- API route: timeout ممتد للملفات الكبيرة

### 2. إعدادات الخادم المطلوبة (لإزالة القيود تماماً)

#### إذا كنت تستخدم Nginx:
```nginx
# في قسم server أو location
client_max_body_size 0;  # لا توجد قيود
client_body_timeout 900s;  # 15 minutes
proxy_read_timeout 900s;
proxy_connect_timeout 900s;
```

#### إذا كنت تستخدم Apache (.htaccess):
```apache
<IfModule mod_php.c>
  php_value upload_max_filesize 0  # لا توجد قيود
  php_value post_max_size 0        # لا توجد قيود
  php_value max_execution_time 0   # لا توجد قيود زمنية
  php_value max_input_time 0       # لا توجد قيود زمنية
</IfModule>

# لا توجد قيود على حجم الطلب
# LimitRequestBody 0
```

#### إذا كنت تستخدم Vercel:
تم تحديث `vercel.json` بـ `maxDuration: 900` (15 minutes)

## المميزات الجديدة

- ✅ **لا توجد قيود على حجم الفيديو** - يمكن رفع فيديوهات بأي حجم
- ✅ **timeout ممتد** - حتى 15 دقيقة للفيديوهات الكبيرة
- ✅ **دعم جميع صيغ الفيديو** - MP4, MOV, AVI, MKV, WEBM, إلخ
- ✅ **تحسين معالجة الأخطاء** - رسائل أوضح عند حدوث مشاكل

## التحقق من الإصلاح

1. أعد نشر التطبيق بعد تطبيق إعدادات الخادم
2. جرب رفع فيديو بحجم كبير (مئات الميجابايت)
3. تحقق من أن الرفع يتم بنجاح بدون أخطاء 413

## ملاحظات مهمة

- ⚠️ **تحذير**: رفع فيديوهات كبيرة جداً قد يستهلك موارد الخادم
- 💡 **نصيحة**: للفيديوهات الكبيرة جداً (>2GB)، قد تحتاج لزيادة إعدادات الخادم أكثر
- 🔄 **الحالة**: الكود جاهز تماماً، المطلوب فقط تحديث إعدادات الخادم
